spring:
  g11n:
    base-directory: i18n-yaml #classpath:i18n todo use classpath
    file-base-name: messages
    file-extension: yml
    locale-separator: _
    default-locale: en
    locales:
      - en
      - de
      - ru
    type: YAML

  config:
    activate:
      on-profile: yaml